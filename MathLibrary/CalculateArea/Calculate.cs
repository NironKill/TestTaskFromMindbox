namespace CalculateArea
{
    public class Calculate
    {
        private readonly Handler _handler;

        public Calculate (Handler handler)
        {
            _handler = handler;
        }

        public double Area(ICollection<double> sides)
        {
            
            double result = 0;

            return result;
        }
    }
}
